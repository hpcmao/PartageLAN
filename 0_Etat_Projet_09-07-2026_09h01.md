# Etat du Projet - PartageLAN
**Date:** 09-07-2026 09h01

App macOS (SwiftUI) de partage entre deux Macs du LAN (10.0.0.4 `vemao` ↔ 10.0.0.5
`hpcmultimedia`) : deux panneaux de navigation façon Transmit, copie de fichiers dans les
deux sens, presse-papier partagé. TCP sur le port **7365**.

## Fonctionnalites implementees
- [x] Deux panneaux (local / distant) avec navigation complète (double-clic, parent, chemin éditable, refresh)
- [x] Copie de fichiers bidirectionnelle (→ / ←, double-clic récupérer, drag & drop, dédoublonnage)
- [x] Presse-papier partagé texte (Les 2 sens / Envoi / Réception / Coupé, sondage 0,6 s, anti-écho)
- [x] Dossier de réception configurable (défaut ~/Downloads)
- [x] Thèmes (Système, Clair, Sombre, Océan, Sépia)
- [x] **Scan réseau** : bouton « Scanner » (panneau distant) → sonde le /24, menu déroulant des Macs détectés, sélection auto si un seul
- [x] **Icône barre de menus macOS** (MenuBarExtra) : statut du pair + actions rapides
- [x] Persistance : chemins des panneaux, dossier de réception, IP pair, thème, géométrie fenêtre (écriture immédiate via didSet UserDefaults)
- [x] Installation/màj en un clic : `PartageLAN.command` (build universel + copie /Applications + LaunchAgent)
- [x] Lancement automatique à la session (LaunchAgent `fr.vemao.partagelan`)

## Structure du projet
```
PartageLAN/
├── Package.swift
├── build_app.sh              # build universel arm64+x86_64 → dist/PartageLAN.app + .zip
├── PartageLAN.command        # install/màj en un clic (git pull, build, /Applications, LaunchAgent)
├── README.md
├── 0_Etat_Projet_*.md
└── Sources/PartageLAN/
    └── PartageLAN.swift       # tout le code (moteur réseau + UI SwiftUI)
```

## Architecture (rappel)
- Transport : `NWListener`/`NWConnection` TCP, port 7365. Trame = `[UInt32 BE longueur][JSON Meta][octets]`.
- Types de messages : `clip`, `file`, `ping`/`pong`, `ls`/`lsr`, `get`, `err`.
- `PartageEngine` (ObservableObject) : moteur réseau + état. `PaneModel` : un panneau.
- Scan : `scanNetwork()` balaie .1–.254 (concurrence bornée à 32 via semaphore), `pingHost()` par hôte (timeout 1 s), collecte les `ScanHost`.
- Presse-papier : `pollClipboard()` (timer 0,6 s, compare `NSPasteboard.changeCount`), `applyRemoteClipboard()`. Anti-boucle via `lastRemoteText` + resync `lastChangeCount`.
- Menu bar : `MenuBarExtra` + `MenuBarContent`.

## Changelog
### 09-07-2026
- Ajout du **scan réseau** (bouton Scanner + menu déroulant, `ScanHost`, `scanNetwork()`, `pingHost()`, `subnetPrefix()`)
- Ajout de l'**icône barre de menus** (`MenuBarExtra` / `MenuBarContent`)
- Tests validés : ping/pong local OK, persistance des chemins OK (cycle fermeture/réouverture)
- Vérification persistance : `localPanePath`, `remotePanePath`, `receiveDir`, `peerIP`, `theme`, géométrie fenêtre

## TODO
- [ ] Tester le scan avec le 2ᵉ Mac réellement présent sur le LAN (protocole validé, reste le test terrain)
- [ ] Vérifier le rendu visuel du menu déroulant scan (multi-Macs) et de l'icône barre de menus
- [ ] Mettre à jour l'app sur le 2ᵉ Mac (double-clic `/Applications/PartageLAN.command` après push)
