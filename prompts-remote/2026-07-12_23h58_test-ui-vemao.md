Objectif : Construire sur CE Mac (vemao, 10.0.0.4) une instance de test des 3 fonctionnalités
ajoutées ce soir côté hpcmultimedia, sans toucher à l'instance de production. Même limitation
habituelle pour le build : `codesign` échoue en session SSH, mais passe en session Claude Code
locale (confirmé plusieurs fois sur ce Mac).

Contexte — ce qui a été fait côté hpcmultimedia (session du 2026-07-12, PAS ENCORE commité) :
1. **Sélection par glisser** (rectangle façon Finder) dans les deux panneaux : clic simple
   (remplace), ⌘-clic (bascule), ⇧-clic (plage), glisser (rectangle), ⌘-glisser (additif).
   Remplace entièrement la gestion de clic native de `List` (qui ne supporte pas le glisser sur
   macOS) par un geste maison `rowGesture(for:)` — double-clic détecté par horodatage, pas par
   un second geste concurrent, pour éviter tout conflit d'arbitrage SwiftUI.
2. **Bouton ⋯▾** (menu déroulant, clic gauche) sur chaque ligne du panneau distant → Terminal
   SSH / Finder, en plus du clic droit existant (menu contextuel, déjà en prod depuis le
   2026-07-10). Les deux déclenchent les mêmes actions.
3. **4 nouveaux thèmes** : Nord, Dracula, Solarisé clair, Contraste élevé (accent orange/ambre
   sur fond quasi noir, pensé daltonisme).

Déjà fait par Claude (hpcmultimedia) avant ce prompt :
- `Sources/PartageLAN/PartageLAN.swift` et `README.md` copiés par scp, vérifiés identiques
  (md5) à la version locale de hpcmultimedia.
- `0_Etat_Projet_10-07-2026_17h29.md` supprimé, remplacé par
  `0_Etat_Projet_12-07-2026_23h45.md` (copié par scp).
- **PAS de commit, PAS de push** — rien n'est encore committé nulle part.
- Sur hpcmultimedia : build release (`build_app.sh`) fait, lancé en second exemplaire via
  `open -n dist/PartageLAN.app` (PID 11681), production (`/Applications/PartageLAN.app`, PID
  718) laissée intacte et non arrêtée. **Rien n'est encore validé par l'utilisateur en usage
  réel** (ni clic/⌘-clic/⇧-clic/glisser, ni double-clic, ni clic droit, ni bouton ⋯▾) — cette
  session est encore au stade « build de test », pas déploiement.

Tâche :
1. `git status --short` + `git diff --stat`. Diff attendu sur `PartageLAN.swift` et `README.md`
   uniquement (le `.gitignore` local à vemao peut apparaître modifié, connu, hors périmètre).
   `0_Etat_Projet_12-07-2026_23h45.md` doit apparaître en fichier non suivi (`??`), l'ancien
   fichier daté du 10-07 ne doit plus exister. Si autre chose diverge, **arrête-toi et signale
   avant de continuer**.
2. `./build_app.sh`. Vérifier le message « Signé avec « PartageLAN Self-Signed » (signature
   stable). ». **Si repli sur signature ad-hoc (message d'avertissement), arrête-toi et
   signale** — ne pas continuer avec une signature ad-hoc (cf. limite connue : casserait la
   stabilité de signature/Little Snitch, cf. mémoire projet).
3. **Ne pas toucher à l'instance de production** (`/Applications/PartageLAN.app`, lancée par le
   LaunchAgent `fr.vemao.partagelan`) : ne pas la tuer, ne pas la remplacer, ne pas toucher au
   LaunchAgent.
4. Lancer une instance de test à côté : `open -n dist/PartageLAN.app`.
5. Vérifier via `lsappinfo list | grep -i -A5 partagelan` que deux entrées apparaissent,
   toutes deux `type="UIElement"` (l'ancienne, bundle path `/Applications/PartageLAN.app` ; la
   nouvelle, bundle path se terminant par `dist/PartageLAN.app`). Noter le PID de la nouvelle.
6. Ne pas simuler de clic réel dans l'UI — se limiter à vérifier que le process de test tourne
   sans crash après 2-3 s (`ps -p <pid>`). L'utilisateur testera lui-même les interactions
   (glisser, ⌘/⇧-clic, double-clic, clic droit, bouton ⋯▾) à la souris.

Contraintes :
- Pas de commit, pas de push (l'utilisateur committera lui-même une fois les deux Macs validés).
- Ne pas régénérer le certificat de signature.
- Ne pas piloter d'autre app via AppleScript/System Events.
- Ne pas toucher à l'instance de production ni au LaunchAgent.
- Ne pas simuler de clic réel dans l'UI.

Livrable : une instance de test de PartageLAN lancée sur vemao (`dist/PartageLAN.app` via
`open -n`), signée avec le certificat stable, à côté de la production intacte — prête à être
testée à la souris par l'utilisateur sur cette machine aussi.

Compte-rendu : créer `prompts-remote/2026-07-12_23h58_test-ui-vemao.result.md` (5-10 lignes) :
étapes passées/ratées, message de signature obtenu, PID de l'instance de test, résultat de la
vérification `lsappinfo`.
